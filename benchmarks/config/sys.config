%% Gater benchmark config. Starts a local bench_store in reckon-db
%% so the gater has something to route to.
[
    {reckon_db, [
        {stores, [
            {bench_store, [
                {data_dir, "data/bench_store"},
                {mode, single}
            ]}
        ]}
    ]}
].
